## AstrBot 断句插件

默认对所有消息生效：对消息中的文本段进行断句，并尽量保留原消息链里的非文本段（图片、语音、At 等），按原顺序返回。

### 指令

无需指令：插件对所有消息自动生效（仅在需要断句时才会响应）。

### 断句规则

- 仅对文本段进行处理：按常见标点（`。！？!?；;，,、`）与换行分句
- 非文本段（如图片/语音/At）不做转换，原样保留

### 安装

1. 将本仓库上传到 GitHub
2. 在 AstrBot 中执行：`plugin i <仓库地址>`
